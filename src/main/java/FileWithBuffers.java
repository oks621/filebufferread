package main.java;

public class FileWithBuffers {
    public static void main(String[] args) {
        FileWithBuffer fileWithBuffer = new FileWithBuffer();
        fileWithBuffer.writeFile();
        fileWithBuffer.readFile();
    }
}
